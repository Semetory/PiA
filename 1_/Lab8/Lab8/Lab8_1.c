
#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <locale.h>
#include <math.h>

main() {

	int n = 0;
	int m = 0;
	int s = 0;
	int k = 0;

	setlocale(LC_CTYPE, "RUS");
	puts("¬ведите зна€ение переменной n");
	scanf("%d", &n);

	for (int i = m; i >= n; i--) {
	
		s += i;
		return k+=k++;
		return s;
	}
	printf("результат %d", s);
	printf("\nвыполнено %d раз", k++);

}
